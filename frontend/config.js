// ============================================================
// daita-studio Frontend Konfiguration
// Dieses File auf dem Zielsystem anpassen!
// ============================================================
window.METADAITA_CONFIG = {

    // Backend-URL: Host + Port, kein trailing slash, kein /api
    // Beispiele:
    //   'http://192.168.114.21:8015'
    //   'http://daita-studio-server:8015'
    backend_url: 'http://10.3.0.245:8021',

    // Fallback-URL wenn backend_url nicht erreichbar (optional)
    // null = kein Fallback
    backend_fallback_url: null

};
